# 安全策略

> 中文 | [English](docs/SECURITY.en.md)

## 支持的版本

| 版本 / 分支 | 安全维护范围 |
|---|---|
| `main` 分支最新代码 | 接受报告并修复经确认的问题 |
| 历史 Release / 旧分支 | 不单独维护或承诺回移安全补丁 |

发现旧版本问题仍可报告，请注明版本和是否在 `main` 复现；无需为了提交报告先升级或在真实数据上重现。修复通常先进入 `main`，再由维护者安排发布。此策略不代表承诺固定修复时间。

## 私密报告渠道

**不要在公开 Issue、PR、讨论或日志附件中披露未修复漏洞、可利用的 PoC 或敏感信息。**

1. 若仓库 Security 页面提供 **Report a vulnerability**，可通过 [GitHub 私密漏洞报告](https://github.com/lucan6290/agentkit/security/advisories/new) 提交
2. 若入口不可见、未启用或无法使用，请发邮件至 `lucan6290@gmail.com`，建议主题为 `[Security] AgentKit - 简要描述`

本文件不代表 GitHub 私密报告功能已经开启；该功能由仓库管理员在 GitHub 设置中管理。普通使用问题请看 [支持指南](SUPPORT.md)。

## 报告应包含的信息

- 受影响版本 / 提交、操作系统与架构
- 问题描述、潜在影响、攻击者需要的权限或用户操作
- 最小复现步骤或使用虚构数据的 PoC，以及预期与实际结果
- 受影响的导入来源、目标工具、global / project 范围、路径或文件类型（如适用）
- 脱敏后的日志片段和可能的修复方向（可选）
- 可联系的方式，以及是否希望公开致谢或保持匿名

不要附带真实 Token、私钥、数据库、第三方个人信息或未经许可访问的数据。无法安全提供复现材料时，先描述问题，与维护者约定后再提供必要证据。

## 关注范围与安全边界

重点关注导入和同步过程中的路径穿越、越界读写、符号链接处理、命令注入、Tauri 权限绕过、凭据泄露，以及更新包真实性校验等问题。

- Skill 与提示词是由用户选择导入的内容，并不因被 AgentKit 管理而自动可信；请先审查来源和内容
- 仍欢迎报告恶意 Skill、仓库文件、链接或外部输入突破应用预期路径 / 权限边界的问题，不能仅因它发生在本机就排除
- 第三方依赖的漏洞若能影响当前项目，请提供受影响版本和实际调用场景；也可同步报告上游
- UI 错误、一般崩溃、性能或功能请求通常走普通 Issue；无法判断是否涉及安全时，优先私密报告

仅在自己拥有或获准测试的环境中进行非破坏性验证，不访问他人数据、不干扰他人系统。此处不提供超出项目控制范围的测试授权或赏金承诺。

## 处理与协调披露

1. 维护者会在有能力处理时确认收到并进行复现、影响分析，必要时请求补充信息；目前没有固定响应或修复 SLA
2. 在修复和可用缓解措施明确之前，请与维护者协调披露时间，避免提前公开可利用细节
3. 经确认的问题会根据影响安排修复；对外发布时在安全公告或 CHANGELOG 中说明影响版本和升级 / 缓解方式
4. 公开致谢应事先征得报告者同意，默认不公开私人联系方式；如需匿名请注明

## 开发和发布安全

- 不提交 `.env`、Token、签名私钥、签名密码或未脱敏的日志 / 数据
- 日常开发不需要发布签名私钥；私钥只由维护者按发布流程安全管理
- 不以禁用认证、路径校验或更新签名验证作为修复方式
- 发现凭据泄露应及时撤销 / 轮换并私密通知维护者；删除文件或改写历史不能替代撤销凭据
- 发布前核验安装包、签名和更新清单，详见 [发布工作流](docs/release-workflow.md)
